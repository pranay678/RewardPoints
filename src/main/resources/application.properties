# Server configuration
server.port=8080

# Logging configuration
logging.level.root=INFO

# Database configuration (if applicable)
spring.datasource.url=jdbc:mysql://localhost:3306/db_name    # Database URL
spring.datasource.username=db_username                      # Database username
spring.datasource.password=db_password                      # Database password

# Other application-specific properties

